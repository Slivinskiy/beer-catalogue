apiVersion: v1
kind: Service
metadata:
  name: beer-catalogue-service
  namespace: beer-catalogue
spec:
  type: NodePort
  selector:
    app: beer-catalogue
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30080
